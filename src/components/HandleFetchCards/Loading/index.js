import React from 'react'
import * as S from './styled'

const Loading = () => {
    return (
        <S.LoadingWrapper>
            <S.Spinner />
        </S.LoadingWrapper>
    )
}

export default Loading
